const prix = 0
const cadeau = ['téléphone', 'airpod', 'pc', 'barre de son']
const minuteur = Math.floor(total_secondes / (60 * 60 * 24));

const cpt = 10;
setInterval(function(){
    --cpt;
    truc.inerHTML = cpt+'';
}, 1000);
